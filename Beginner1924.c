#include <stdio.h>
#include <string.h>

int main(){

    int n, i;
    char course[50];

    scanf("%d", &n);

    for(i = 1; i <= n; i++){

        gets(course);
        getchar();
    }

    printf("Ciencia da Computacao\n");

    return 0;
}
